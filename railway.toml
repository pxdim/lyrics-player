# Railway deployment configuration
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.controller"

[deploy]
healthcheckPath = "/"

[controller.service]
name = "lyrics-player-controller"
source = "."
